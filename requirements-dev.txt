# Dependencias de desarrollo para NaturalSQL
# Instalar con: pip install -r requirements-dev.txt

# Dependencias core de la libreria (tambien en pyproject.toml)
chromadb>=0.5.0
sentence-transformers>=2.2.0

# Drivers de BD (opcionales, instalar segun necesidad)
psycopg2-binary>=2.9.0
pymysql>=1.1.0
# pyodbc>=5.1.0  # Descomentar si se prueba SQL Server

# Herramientas para pruebas locales
groq>=1.0.0

# Build tools
build>=1.0.0
setuptools>=68
wheel
